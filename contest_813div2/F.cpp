#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
#define int ll

int solve(){

    int n;
    if (!(cin>>n))return 1;
    
    return 0;
    
}

signed main(){
    ios::sync_with_stdio(0);cin.tie(0);
    int t=1e9;
    // cin>>t;
    while (t--){
        if (solve())break;
    }
    cerr<<"time: "<<(double)clock() / (double)CLOCKS_PER_SEC<<"\n";
}