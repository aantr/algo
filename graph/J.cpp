#include <bits/stdc++.h>
using namespace std;
// #define int long long

int solve() {

	int k = 1;
	set<int> s;
	for (int i = 0; i < 1e6; i++) {
		// s.insert(i);
		k++;
	}
	return 1;
}

signed main () {
	int t = 1e9;
	while(t--)if(solve()) break;
	cerr << "Time: " << (double) clock() / CLOCKS_PER_SEC << "\n";
}